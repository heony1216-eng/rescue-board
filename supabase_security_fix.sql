-- =====================================================
-- ⚠️ 이 파일은 더 이상 사용하지 마세요!
-- =====================================================
-- 이 파일은 이전 버전의 평문 비밀번호 비교 함수들이 포함되어 있었습니다.
-- 보안 문제로 인해 모든 함수가 bcrypt 해시 기반으로 변경되었습니다.
--
-- 대신 다음 파일을 사용하세요:
--   - create_tables_first.sql (최초 설정)
--   - supabase_security_migration.sql (보안 마이그레이션)
--
-- 이 파일의 함수들은 보안 취약점(평문 비밀번호 비교)이 있으므로
-- 절대 실행하지 마세요.
-- =====================================================
